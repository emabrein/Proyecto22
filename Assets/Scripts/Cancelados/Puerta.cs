using System.Collections;
using System.Collections.Generic;
using UnityEngine;

 public class Puerta : MonoBehaviour
 {
    // [SerializeField] private GameObject[] waypoints ;
    //  private int currenWaypointIndex = 0; // 0 es igual a ON. 1 es igual a OFF

    // [SerializeField] private float speed = 0.5f; 

       

    //  public static void AbrirPuerta()
    // {
    //      transform.position = Vector2.MoveTowards(transform.position, waypoints[currenWaypointIndex].transform.position, Time.deltaTime * speed);
    //      currenWaypointIndex++;
    // }

    //  public static void CerrarPuerta()
    //  {
    //      transform.position = Vector2.MoveTowards(transform.position, waypoints[currenWaypointIndex].transform.position, Time.deltaTime * speed);
    //      currenWaypointIndex = 0;
    //  }

 }
    